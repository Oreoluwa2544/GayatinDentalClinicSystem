-The SQL file is named clinic inside clinic, before everything this must be set up.

-In order to log in as a clinic admin "clinic/admin" is the url.

-A pre registered admin account is created for you: username and password is "admin".

